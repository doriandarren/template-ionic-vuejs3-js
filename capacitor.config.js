import { CapacitorConfig } from '@capacitor/cli';

const config = {
  appId: 'eu.globaldevelopers.globalfleet',
  appName: 'fleetDriver',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
